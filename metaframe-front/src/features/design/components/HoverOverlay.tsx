// - Role: Defines a frontend implementation unit.
// - Notes: This file supports the surrounding feature package and screen flow.

// HoverOverlay gives a lighter affordance than selection so users can preview targeting before clicking.
export function HoverOverlay() {
  return <div className="design-canvas__hover-badge">Hover</div>
}
